# history has no models of its own — it's a read-only aggregation layer over
# apps.journey.JourneySummary, apps.accounts.SosLog, apps.community.IncidentReport,
# and apps.ai_assistant.AiRecommendationLog. Nothing to define here.
